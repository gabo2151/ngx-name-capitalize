name: Node.js Package

on:
  release:
    types: [created]
  push:
    branches: [v1]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  publish-npm:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/

      # Trusted Publishing (OIDC) needs npm >= 11.5.1; Node ships npm 10.x.
      # Pin to the 11.x line: it supports OIDC and runs on Node 22, while
      # npm@latest (12.x) requires a very recent Node 22.22+/24.15+.
      # (This runner Node is independent from the library's Node >= 16
      # support: publishing only uploads the built dist/, it does not run it.)
      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@11

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npx -p @angular/cli@12 ng build ngx-name-capitalize

      - name: Publish to NPM
        run: npm publish --provenance --access public --tag legacy-v1
        working-directory: dist/ngx-name-capitalize
